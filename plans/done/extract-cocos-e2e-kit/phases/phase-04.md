# Phase 04 - Re-Adopt Kit On Main And Restore E2E

## Status

Completed.

## Objective

在本 repo `main` 建立最終導入結果：保留 `frontend/` 與 `server/` 結構，依 `cocos-e2e-kit` README 導入 framework，並恢復/調整現有三條 demo E2E 測項。

## Inputs

- `demo-without-e2e` branch 結果
- `starazzip/cocos-e2e-kit` kit repo
- 現有 demo E2E case 與 automation class
- 決策中的三條保留測項

## Scope

- 回到 `main`。
- 將 demo repo 調整成與 `demo-without-e2e` 相同的 frontend/server 基礎結構。
- 從 kit 導入 framework。
- 恢復三條 demo E2E。
- 調整路徑以支援 `frontend/`。
- 更新 scripts 與 Playwright config。
- 驗證 E2E 可列出與執行。

## Out Of Scope

- 不改 kit repo 交付形式。
- 不新增大量新測項。
- 不建立 CI/CD。
- 不 force push。

## Plan

1. 確認 `main` 工作樹乾淨。
2. 將 frontend/server 結構與 playable branch 對齊。
3. 透過 kit scaffold 或依 kit README 導入 framework。
4. 放回 demo-specific project adapter：
   - `demo-backend`
   - frontend-only demo fixture params
5. 放回 E2E case metadata 與 Cocos automation class。
6. 調整所有路徑到 `frontend/` 結構。
7. 更新 root README 的 final result 說明。
8. 跑 unit tests、test list、demo E2E。
9. Commit `main`。

## BDD

- Given 使用者 checkout `main`  
  When 依 README 開啟 Cocos Preview 並執行 E2E  
  Then 應可看到並執行三條 demo E2E。

- Given 使用者比較 `demo-without-e2e` 與 `main`  
  When 閱讀 README  
  Then 能理解 main 是照 kit README 導入後的完成狀態。

## TDD

- 跑 framework unit tests。
- 跑 demo adapter tests。
- 確認 discovery tests 對 `frontend/` 新路徑仍有效。

## Implement

- 導入 kit scaffold。
- 加入 demo-specific adapter 與 demo E2E。
- 調整 Cocos/Playwright scripts path。
- 更新 README。

## E2E

- `npx playwright test --list`
- `npx playwright test -g "frontend-only deterministic spin"`
- `npx playwright test -g "forced board spin payout"`
- `npm run test:e2e` 或對應新路徑 script

## Review

- 檢查 framework code 是否只透過 kit 導入，不再有不明來源複製。
- 檢查 demo-specific adapter 未混入 kit core。
- 檢查 README 與實際指令一致。

## Fix

- 修正路徑、adapter、scripts 或 README drift。
- 若 Cocos Preview 需要人工步驟，明確寫入 README/smoke。

## Wrap Up

- 記錄 main commit hash。
- 記錄 E2E 驗證結果。
- 記錄與 kit README 對應的導入步驟。

## Results

### Main Working State

- Cocos project root moved to `frontend/`.
- Go backend remains in `server/`.
- Root no longer contains Cocos project/E2E runtime directories:
  - `assets/`
  - `settings/`
  - `extensions/`
  - `tools/`
  - `tests/`
  - `playwright.config.mjs`
  - root `package.json`
- `frontend/extensions/cocos-e2e-framework/` was copied from `D:\Azzip\cocos\cocos-e2e-kit`.
- `frontend/extensions/automation-framework/` was generated by the kit scaffold.
- Three demo E2E cases remain under `frontend/tests/e2e/cases/`.
- Demo automation classes remain under `frontend/assets/e2e/`.
- Demo backend adapter remains project-specific in `frontend/tools/e2e/project-environment-adapters.mjs`.

### Demo Path Adjustments

- `frontend/tools/e2e/project-environment-adapters.mjs` starts the Go backend from `../server`.
- `frontend/tools/demo-backend.mjs` starts the Go backend from `../server`.
- `frontend/tools/e2e/project-environment-adapters.test.mjs` now asserts the new backend cwd.

### Review

- Findings: None for runtime/framework adoption.
- Documentation still needs to be rewritten for the new two-repo/two-branch structure; this is Phase 05 scope.

### Verification

- Scaffold check for `frontend/`: passed.
- `npm run test:e2e:unit` in `frontend/`: passed, 37 tests.
- `npx playwright test --list` in `frontend/`: listed 3 tests.
- `npx playwright test -g "frontend-only deterministic spin"` in `frontend/`: passed.
- `npx playwright test -g "forced board spin payout"` in `frontend/`: passed.
- `npm run test:e2e` in `frontend/`: passed, 3 tests.
- `go test ./...` in `server/`: passed.
- `git diff --check`: passed.

## Verification

- `npm run test:e2e:unit` 或新 script
- kit/template tests 若仍在 repo 中
- `npx playwright test --list`
- `npm run test:e2e`
- `git diff --check`

## Done Criteria

- `main` 是導入後最終結果。
- `frontend/` 與 `server/` 結構清楚。
- 三條核心 E2E 可列出並通過。
- 無 force push。
