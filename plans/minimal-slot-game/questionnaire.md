# Minimal Slot Game Questionnaire

請選擇每題最符合的答案。未回答的題目會使用標記為「建議預設」的選項。每題只有一個 `Other:`，若選 Other 請在同一行寫清楚內容。

1. 這個 MVP 主要給誰使用？
   - [ ] A. 開發者自己驗證前後端流程（建議預設）
   - [X] B. 內部展示給團隊看
   - [ ] C. 給玩家試玩但不涉及真金流
   - [ ] Other:

2. 最重要的完成結果是什麼？
   - [ ] A. 可以從 Cocos 操作開分、洗分、Spin、Auto，並由 Go 後端回傳盤面與餘額（建議預設）
   - [ ] B. 優先做出視覺上比較像正式老虎機的畫面
   - [ ] C. 優先做後端規則與資料庫，不要求前端完整互動
   - [X] Other: 除了A外 更重要的是automation-testing 的e2e 測試 可以執行開分、spin 並驗證金額與盤面是否正確

3. 3x3 盤面的顯示方式要多簡單？
   - [X] A. 用 Cocos 內建 Label/Color/Sprite 顯示符號文字即可（建議預設）
   - [ ] B. 使用簡單幾何圖形或色塊區分符號
   - [ ] C. 需要基本動畫，但可用簡單閃爍或切換
   - [ ] Other:

4. 開分功能要怎麼定義？
   - [X] A. 輸入或點擊固定加分，把玩家餘額增加並寫入 SQLite（建議預設）
   - [ ] B. 每次重開遊戲自動補到固定餘額
   - [ ] C. 只做前端按鈕，不需要後端持久化
   - [ ] Other:

5. 洗分功能要怎麼定義？
   - [X] A. 把目前餘額歸零或轉出，後端回傳已洗出分數並寫入 SQLite（建議預設）
   - [ ] B. 只扣除指定數量分數
   - [ ] C. 只做 UI 顯示，不改資料庫
   - [ ] Other:

6. Spin 的押注規則要多簡單？
   - [ ] A. 固定每次押 1 分，不做押注調整（建議預設）
   - [X] B. 提供幾個固定押注選項
   - [ ] C. 允許玩家輸入押注
   - [ ] Other:

7. 盤面設定要支援到什麼程度？
   - [X] A. 後端有 SQLite/JSON 可持久化的符號、輪帶、賠付表設定，啟動時載入預設值（建議預設）
   - [ ] B. 只需要硬編碼在 Go domain 裡
   - [ ] C. 需要前端 UI 可以即時修改盤面設定
   - [ ] Other:

8. 中獎判定要採用哪種最小規則？
   - [X] A. 只判斷三條橫線，三個相同符號才中獎（建議預設）
   - [ ] B. 判斷 5 條線：3 橫、2 斜
   - [ ] C. 只隨機給 win，不實作 payline
   - [ ] Other:

9. Auto Spin 要做到什麼程度？
   - [X] A. 按 AUTO 後連續 Spin，再按一次停止；餘額不足自動停（建議預設）
   - [ ] B. 固定跑 10 次後停止
   - [ ] C. 只需要前端模擬，不必真的連續呼叫後端
   - [ ] Other:

10. 前後端通訊方式？
    - [X] A. WebSocket envelope，包含 request id、type、payload（建議預設）
    - [ ] B. REST API
    - [ ] C. 先本機 mock，之後再接 WebSocket
    - [ ] Other:

11. SQLite 需要保存哪些資料？
    - [X] A. 玩家餘額、交易紀錄、spin 紀錄、盤面/賠付設定（建議預設）
    - [ ] B. 只保存玩家餘額
    - [ ] C. 只保存 spin 紀錄
    - [ ] Other:

12. 目前是否需要登入或多玩家？
    - [X] A. 不需要，使用單一本機玩家 id 即可（建議預設）
    - [ ] B. 需要簡單玩家 id 輸入
    - [ ] C. 需要登入流程
    - [ ] Other:

13. 安全與權限邊界？
    - [X] A. 不涉及真金流；仍由後端驗證餘額與請求，不信任前端（建議預設）
    - [ ] B. 只是展示，可接受前端決定結果
    - [ ] C. 需要接近正式金流的審計與權限設計
    - [ ] Other:

14. 驗證方式要到哪裡？
    - [] A. Go domain/app 單元測試，加上基本前後端手動 smoke steps（建議預設）
    - [ ] B. 只要手動測試
    - [ ] C. 需要 Playwright/Cocos 自動化 E2E
    - [X] Other: GO單元測試+C(希望可以用aumation-testing套件完成)

15. 不在 MVP 範圍內的是什麼？
    - [X] A. 真實美術、音效、正式營運後台、登入、真金流、多語系（建議預設）
    - [ ] B. 只排除真金流，其它都希望做
    - [ ] C. 只做後端，不做前端 polish
    - [ ] Other:

16. 可接受的主要取捨？
    - [X] A. 先可玩、可持久化、規則清楚，畫面保持樸素（建議預設）
    - [ ] B. 先畫面好看，後端可以簡化
    - [ ] C. 先後端完整，前端可以很粗略
    - [ ] Other:
